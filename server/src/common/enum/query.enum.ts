export enum SortByEnum {
    DESC = 'DESC',
    ASC = 'ASC',
}

export enum OperatorEnum {
    IN = 'in',
    CONTAINS = 'contains',
    GREATER_OR_EQUAL = '>=',
    GREATER = '>',
    SAME = '=',
    SMALLER = '<',
    SMALLER_OR_EQUAL = '<=',
}

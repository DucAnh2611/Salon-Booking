import { settingConfig } from '../../config/setting.config';

export const SETTING_DEFAULT = {
    otpVerifyEmail: settingConfig.otpVerifyEmail,
    orderServiceConfirm: settingConfig.orderServiceConfirm,
    resetPassword: settingConfig.resetPassword,
};

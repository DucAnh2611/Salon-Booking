<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            color: #333;
        }
        table {
            width: 100%;
            max-width: 600px;
            margin: 20px auto;
            border-collapse: collapse;
        }
        td {
            padding: 10px;
            border: 1px solid #ddd;
        }
        a {
            display: inline-block;
            padding: 10px 20px;
            color: #fff;
            background-color: #007bff;
            text-decoration: none;
            border-radius: 5px;
        }
        .redirect {
            margin-top: 20px;
            background-color: #4CAF50;
            color: white;
            text-align: center;
            border: none;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
        }
        @media (max-width: 600px) {
            td {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <td>
                {{app_name}}
            </td>
        </tr>
        <tr>
            <td>
                Hello {{name}},<br>
                Please verify your email address by entering the OTP below.
            </td>
        </tr>
        <tr>
            <td style="text-align:center">
                Your OTP: <strong>{{otp}}</strong><br>
                <a href="{{redirectURL}}" class="redirect">Quick Verify</a>
            </td>
            
        </tr>
    </table>
</body>
</html>